// src/app/company/layout.js
export const metadata = {
  title: 'Company Admin - KeepMyKit',
  description:
    'Company admin panel for managing device sale offers and employee transactions',
};

export default function CompanyLayout({ children }) {
  return children;
}
